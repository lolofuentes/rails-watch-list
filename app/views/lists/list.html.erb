<div class= "navbar" >
<a>Revisa más títulos</a>
</div>
<h2><% list.movies.each do |movie| %></h2>

<ul>
  <%   @lists.each do |list| %>
    <li>
    <%= list.name %>
    </li>
  <% end %>
</ul> 


<%= link_to("volver", list_path)%>
